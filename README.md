# Fitness

Simple spring-boot rest-application.

## Run

Command line:

``mvn clean install``

and then

``mvn spring-boot:run``

Open with browser [swagger-ui](http://localhost:8090/swagger-ui.html), where you can try rest.

